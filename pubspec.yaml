name: pulsing_button
version: 0.0.1
description: A pulsing button for Flutter
repository: "https://github.com/nubcake94/pulsing_button"

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter